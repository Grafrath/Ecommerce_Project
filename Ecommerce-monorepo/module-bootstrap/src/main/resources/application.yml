# 1. Next.js 3000? ?? ?? ??
server:
  port: 8080

spring:
  # 2. H2 ?????? ? ??? ??? ?? ??
  datasource:
    # ??: ??? ?? ? H2 ???? 3?? ???(?)? ??? ?????.
    url: jdbc:h2:mem:ecommerce;INIT=CREATE SCHEMA IF NOT EXISTS CORE\; CREATE SCHEMA IF NOT EXISTS CATALOG\; CREATE SCHEMA IF NOT EXISTS SUPPORT;
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # 3. JPA ?? ? ?? ??
  jpa:
    hibernate:
      # ??? ?? ? ??? ???? ??? ???? ???? ????
      ddl-auto: update
    show-sql: true # ???? ??? ??? ?????.
    properties:
      hibernate:
        format_sql: true # ??? ?? ?? ???? ???.
        default_schema: CORE # ???? ??? ??? ???? ? ??? ??? ???? ?? ?

  # 4. Redis ?? ?? (???? ? ??? ??? Pub/Sub ??)
  data:
    redis:
      host: localhost
      port: 6379