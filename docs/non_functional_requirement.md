# 비기능적 요구사항 (Non-Functional Requirements)

본 문서는 이커머스 시스템이 충족해야 하는 성능, 신뢰성, 보안 등의 품질 목표를 정의하고 추적합니다.

## 1. 데이터 정합성 및 신뢰성 (Reliability)

| 요구사항 | 상세 설명 및 목표 기준 | 검증 및 구현 방안 | 상태 |
| :--- | :--- | :--- | :--- |
| **동시성 제어** | 다수의 사용자가 동시에 한정 수량 상품을 주문할 때, 실제 재고 이상의 초과 판매(Over-selling) 완전 차단 | DB Lock (비관적/낙관적 락) 또는 Redis 분산 락 적용 후 성능 테스트 도구(JMeter 등)를 통한 동시성 검증 | `TODO` |
| **분산 트랜잭션 롤백** | 주문-재고-프로모션-결제 도메인 간 통신 중 하나의 단계라도 실패할 경우, 이미 처리된 데이터(재고 차감, 쿠폰 사용 등)를 100% 이전 상태로 복구 | 보상 트랜잭션 로직 구현 및 각 도메인 실패 시나리오에 대한 통합 테스트(Integration Test) 작성 | `TODO` |

## 2. 성능 및 가용성 (Performance & Availability)

| 요구사항 | 상세 설명 및 목표 기준 | 검증 및 구현 방안 | 상태 |
| :--- | :--- | :--- | :--- |
| **API 응답 속도** | 트래픽이 집중되는 상품 전시 및 검색(Catalog) API의 평균 응답 시간을 200ms 이하로 유지 | 실행 계획(Explain) 분석을 통한 인덱스 튜닝 및 Redis 로컬/글로벌 캐싱 도입 | `TODO` |
| **장애 격리 (Fault Tolerance)** | 알림(메일 발송) 등 비핵심 도메인의 지연이나 장애가 핵심 트랜잭션(주문/결제) 처리에 영향을 주지 않아야 함 | 메일 발송 로직은 타 도메인 호출과 분리하여 비동기(메시지 큐 또는 `@Async`)로 처리 및 Timeout 설정 | `TODO` |

## 3. 보안 (Security)

| 요구사항 | 상세 설명 및 목표 기준 | 검증 및 구현 방안 | 상태 |
| :--- | :--- | :--- | :--- |
| **데이터 암호화** | 사용자 비밀번호는 복호화가 불가능한 단방향 해시로 저장하고, 이름/전화번호 등 개인정보는 양방향 암호화하여 저장 | Spring Security `BCryptPasswordEncoder` 적용 및 개인정보 컬럼 대상 AES-256 암호화 로직 구현 | `TODO` |
| **Stateless 인증** | 서버 확장에 유연하게 대응하고 세션 불일치 문제를 방지하기 위해 서버의 상태를 유지하지 않는 인증 구조 설계 | 세션 생성 옵션 비활성화 및 JWT(JSON Web Token) 기반의 인증/인가 필터 구현 | `TODO` |

## 4. 관측성 (Observability)

| 요구사항 | 상세 설명 및 목표 기준 | 검증 및 구현 방안 | 상태 |
| :--- | :--- | :--- | :--- |
| **분산 환경 로깅** | 클라이언트의 단일 요청이 여러 도메인을 거칠 때, 전체 흐름을 하나의 식별자로 추적 가능해야 함 | API 최초 요청 진입 시 고유 식별자(Trace ID)를 생성하여 로그에 추가 (Logback MDC 등 활용) | `TODO` |